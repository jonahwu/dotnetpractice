namespace app_lib
{
    public class testlib
    {
        
        // if just used as libary static is must setting.
        // without static, it cannot be called.
        public static void Output(string msg){
            Console.WriteLine(msg);
        }
    }
}